import express from "express";


const router = express.Router();

router.get("/users", );
router.post("/users", );
router.put("/users/:id", );
router.delete("/users/:id", );
router.post("/users/:id/link",);
router.delete("/users/:id/unlink",);
router.get("/graph", );

export default router;
